---

---

<div class="skip-to-bottom-wrapper" title="Xuống cuối trang">
  <div class="skip-to-bottom-container">
    <a href="#bottom" aria-label="skip_to_bottom" class="skip-to-bottom-btn">
      <span class="skip-to-bottom-icon"><span class="skip-to-bottom-arrow">⬇️</span></span>
    </a>
  </div>
</div>

<script>
  document.addEventListener("scroll", () => {
    const btn = document.querySelector(".skip-to-bottom-btn");
    if (!btn) return;

    if (window.scrollY > 300) {
      btn.style.opacity = "0";
    } else {
      btn.style.opacity = "1";
    }
  });
</script>

<!--<style>
  /* wrapper = sticky bottom bar */
  .skip-to-bottom-wrapper {
    pointer-events: none;
    position: sticky;
    top: 0;
    right: 0;
    z-index: 40;
    //margin-top: -2rem; /* -mb-8 */
    height: 2rem; /* h-8 */
    width: 100%; /* w-full */
  }

  /* container = flex box to right align */
  .skip-to-bottom-container {
    position: absolute;
    top: 0;
    display: flex;
    justify-content: flex-end;
    max-width: 30px; /* container-xl (approx Tailwind’s max width) */
    margin: 0 auto;
    left: 0;
    right: 0;
  }

  /* button styling */
  .skip-to-bottom-btn {
    pointer-events: auto;
    display: grid;
    place-items: center;
    height: 2.5rem; /* h-10 */
    width: 2.5rem;  /* w-10 */
    transform: translateY(-1rem); /* -translate-y-4 */
    opacity: 1; /* initially hidden */
    border-radius: 0.5rem;
    text-decoration: none;
    background: none; /* surface-primary fallback */
    box-shadow: 0 1px 2px rgba(0,0,0,0.1);
    transition: opacity 0.3s ease;
  }

  /* icon rotation */
  /*    .skip-to-bottom-icon {
          height: 1.25rem; !* h-5 *!
          width: 1.25rem; !* w-5 *!
          transform: rotate(-90deg);
      }*/

  /* arrow group translate animation */
  .skip-to-bottom-arrow {
    transform: translateX(-20%);
    transition: transform 0.3s ease;
  }
  .skip-to-bottom-btn:hover .skip-to-bottom-arrow {
    transform: translateX(0);
  }

  /* line path scaling animation */
  .skip-to-bottom-line {
    transform-origin: right;
    transform: translateX(-8%) scaleX(0);
    transition: transform 0.3s ease;
  }
  .skip-to-bottom-btn:hover .skip-to-bottom-line {
    transform: scaleX(0.75);
  }
</style>-->



<!--<a href="#bottom" class="skip-to-bottom-btn" aria-label="go_to_bottom" title="Xuống cuối trang">
  ⬇️
</a>-->


<style>
    .skip-to-bottom-btn {
        pointer-events: auto;
        display: grid;
        place-items: center;
        height: 2.5rem;
        width: 2.5rem;
        border-radius: 0.5rem;
        background: none;
        box-shadow: 0 1px 2px rgba(0,0,0,0.1);
        text-decoration: none;
        transition: opacity 0.3s ease;
        opacity: 1;
        position: fixed;
        top: 5rem;
        right: 0;
    }

</style>

